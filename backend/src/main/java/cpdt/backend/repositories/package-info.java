/**
 * Defines the data access layer for the Digital Twin backend.
 *
 * <p>This package contains Spring Data JPA repositories that provide
 * database access for the application's persistent entities. These
 * repositories abstract database operations and enable the service
 * layer to retrieve and store application data.
 *
 */
package cpdt.backend.repositories;