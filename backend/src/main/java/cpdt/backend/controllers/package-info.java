/**
 * Provides REST controllers for the Digital Twin backend.
 *
 * <p>This package exposes HTTP endpoints that allow external clients to
 * interact with the backend services. The controllers provide access to
 * device information, telemetry history, Digital Twin state, and alarm
 * management while delegating business logic to the service layer.
 *
 * <p>These controllers follow the REST architectural style and act as the
 * entry point for client requests to the backend application.
 *
 */
package cpdt.backend.controllers;