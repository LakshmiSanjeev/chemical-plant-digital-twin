/**
 * Defines the persistent entities used by the Digital Twin backend.
 *
 * <p>This package contains Java Persistence API (JPA) entity classes that
 * represent the application's relational database model. These entities
 * store device metadata, telemetry measurements, alarm thresholds, and
 * generated alerts required for Digital Twin operation.
 *
 * <p>Each entity is mapped to a corresponding database table and serves
 * as the persistence model used by the repository and service layers.
 *
 */

package cpdt.backend.entities;