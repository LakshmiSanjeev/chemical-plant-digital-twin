/**
 * Provides Spring configuration classes for the backend module.
 *
 * <p>This package contains configuration components responsible for
 * initializing application infrastructure, including MQTT client
 * configuration, messaging channels, and supporting configuration
 * objects used by the Digital Twin backend.
 *
 * <p>These classes define application beans and externalized
 * configuration required for telemetry ingestion and communication
 * with external systems.
 *
 */
package cpdt.backend.config;